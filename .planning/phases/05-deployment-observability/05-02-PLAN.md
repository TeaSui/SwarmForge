---
wave: 2
depends_on: ["05-01"]
files_modified:
  - Dockerfile
  - infra/swarmforge_stack.py
  - .dockerignore
autonomous: true
---

# Plan 05-02: Docker & ECS Task Configuration

**Phase:** 5 — Deployment & Observability
**Wave:** 2 (depends on infra setup)
**Requirements:** INF-05

## Goal

Create a production-ready Docker image for SwarmForge and configure the ECS Fargate service to run it.

## must_haves

- Optimized `Dockerfile` based on `python:3.11-slim`.
- `.dockerignore` to exclude development artifacts.
- EFS volume mount configured in the Task Definition.
- ECS Fargate service with auto-scaling and health checks.
- Logging configured to CloudWatch.

## Tasks

<task id="1">
Write production `Dockerfile` and `.dockerignore`.
</task>

<task id="2">
Update `infra/swarmforge_stack.py` with `ApplicationLoadBalancedFargateService`.
</task>

<task id="3">
Configure EFS mount points in the Task Definition.
</task>

## Verification
- Docker image builds locally.
- `cdk deploy` (simulated check) correctly maps ECR image and EFS volume.
